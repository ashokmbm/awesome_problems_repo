package com.practise.datastructure.heap;
/*
     Heap is priority queue where most priority element will be remove first
     We can represent heap as complete binary tree (except leaf nodes) where parent node always have higher priority than it children.
     Leaf nodes will be added from left to right direction.
     Two type of heaps Min Heap / Max Heap
     We can implement heap by using array list to ful fill property of left to right filling of leaf nodes.

 */

public class CustomHeap {

}
